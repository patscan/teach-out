<div>
  <%= form_for @message do |t| %>
  <ul>
  <% @students.each do |student| %>
      <%= check_box_tag "students[#{student.first_name}]", student.id %> <%= student.first_name %>
    <% end %>
  </ul>


  <%= t.label :content %>
  <%= t.text_area :content %>

  <%= t.submit %>
  <% end %>

</div>




